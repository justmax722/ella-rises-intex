<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Make a Donation - Ella Rises</title>
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-background">
    <!-- Header -->
    <header class="w-full px-6 py-4 md:px-8 md:py-6 bg-muted border-b border-border">
        <div class="container mx-auto flex items-center justify-between">
            <div class="flex items-center">
                <a href="/" class="block">
                    <img src="/images/logo.png" alt="Ella Rises" class="h-8 md:h-10 w-auto object-contain" style="max-width: 200px;">
                </a>
            </div>
            <div class="flex items-center gap-3 md:gap-4">
                <a href="/donate" class="px-4 py-2 md:px-6 md:py-2.5 bg-sage text-sage-foreground rounded-md font-medium hover:bg-sage/80 transition-colors">
                    Donate
                </a>
                <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn === true) { %>
                <a href="/home" class="px-4 py-2 md:px-6 md:py-2.5 bg-primary text-primary-foreground rounded-md font-medium hover:bg-primary/90 transition-colors">
                    Dashboard
                </a>
                <% } else { %>
                <a href="/login" class="px-4 py-2 md:px-6 md:py-2.5 bg-primary text-primary-foreground rounded-md font-medium hover:bg-primary/90 transition-colors">
                    Login
                </a>
                <% } %>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="w-full px-6 py-8 md:py-10 lg:py-10">
        <div class="container mx-auto max-w-2xl">


            <!-- Back to Home Button -->
            <div class="mb-6">
                <a href="/" class="inline-flex items-center gap-2 text-muted-foreground hover:text-foreground transition-colors">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-4 w-4">
                        <path d="m12 19-7-7 7-7"></path>
                        <path d="M19 12H5"></path>
                    </svg>
                    <span>Back to Home</span>
                </a>
            </div>

            <!-- Donation Form Card -->
            <div class="bg-card rounded-lg p-6 md:p-8 shadow-sm">
                <!-- Title Section (moved inside card like login page) -->
                <div class="flex flex-col space-y-1.5 mb-6 text-center">
                    <div class="flex justify-center mb-4">
                        <div class="w-16 h-16 rounded-full border-2 border-accent flex items-center justify-center bg-accent/10">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-accent" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd" />
                            </svg>
                        </div>
                    </div>
                    <h1 class="text-3xl md:text-4xl font-serif font-bold text-card-foreground">
                        Make a Donation
                    </h1>
                    <p class="text-sm text-card-foreground/70">
                        Your generosity helps us continue empowering young women through education and leadership programs.
                    </p>
                </div>

                <!-- Success Message -->
                <% if (typeof query !== 'undefined' && query.success === 'true') { %>
                <div class="mb-6 p-4 bg-secondary/20 border border-secondary rounded-md">
                    <p class="text-sm text-card-foreground text-center">
                        Thank you for your donation! Your support makes a difference.
                    </p>
                </div>
                <% } %>

                <form action="/donate" method="POST" class="space-y-6">
                    <!-- Logged-in user message -->
                    <% if (typeof loggedIn !== 'undefined' && loggedIn === true) { %>
                    <div class="mb-4 p-4 bg-secondary/20 border border-secondary rounded-md">
                        <p class="text-sm text-card-foreground text-center">
                            Donating as: <strong><%= userEmail %></strong>
                        </p>
                    </div>
                    <!-- Hidden input for logged-in user email -->
                    <input type="hidden" name="loggedInUserId" value="<%= userId %>">
                    <% } %>

                    <!-- Donation Amount -->
                    <div>
                        <label class="block text-sm font-medium text-card-foreground mb-3">
                            Donation Amount <span class="text-destructive">*</span>
                        </label>
                        <div class="grid grid-cols-2 sm:grid-cols-5 gap-3 mb-4">
                            <button 
                                type="button" 
                                data-amount="25"
                                class="donation-amount-btn px-4 py-2.5 border border-input rounded-md bg-background text-foreground font-medium hover:bg-muted transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                $25
                            </button>
                            <button 
                                type="button" 
                                data-amount="50"
                                class="donation-amount-btn px-4 py-2.5 border border-input rounded-md bg-background text-foreground font-medium hover:bg-muted transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                $50
                            </button>
                            <button 
                                type="button" 
                                data-amount="100"
                                class="donation-amount-btn px-4 py-2.5 border border-input rounded-md bg-background text-foreground font-medium hover:bg-muted transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                $100
                            </button>
                            <button 
                                type="button" 
                                data-amount="250"
                                class="donation-amount-btn px-4 py-2.5 border border-input rounded-md bg-background text-foreground font-medium hover:bg-muted transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                $250
                            </button>
                            <button 
                                type="button" 
                                data-amount="500"
                                class="donation-amount-btn px-4 py-2.5 border border-input rounded-md bg-background text-foreground font-medium hover:bg-muted transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                $500
                            </button>
                        </div>
                        <div class="relative">
                            <span class="absolute left-4 top-1/2 -translate-y-1/2 text-foreground font-medium">$</span>
                            <input 
                                type="number" 
                                id="donationAmount" 
                                name="donationAmount" 
                                required
                                min="1"
                                step="0.01"
                                placeholder="Other amount"
                                value="<%= typeof query !== 'undefined' && query.amount ? query.amount : '' %>"
                                class="w-full pl-8 pr-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>
                    </div>

                    <!-- Payment Method -->
                    <div>
                        <label for="paymentMethod" class="block text-sm font-medium text-card-foreground mb-2">
                            Payment Method <span class="text-destructive">*</span>
                        </label>
                        <div class="relative">
                            <select 
                                id="paymentMethod" 
                                name="paymentMethod" 
                                required
                                class="w-full px-4 py-2.5 pr-10 border border-input rounded-md bg-background text-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors appearance-none cursor-pointer"
                            >
                            <option value="">Select payment method</option>
                            <option value="credit" <%= typeof query !== 'undefined' && query.paymentMethod === 'credit' ? 'selected' : '' %>>Credit Card</option>
                            <option value="debit" <%= typeof query !== 'undefined' && query.paymentMethod === 'debit' ? 'selected' : '' %>>Debit Card</option>
                            <option value="paypal" <%= typeof query !== 'undefined' && query.paymentMethod === 'paypal' ? 'selected' : '' %>>PayPal</option>
                            <option value="bank" <%= typeof query !== 'undefined' && query.paymentMethod === 'bank' ? 'selected' : '' %>>Bank Transfer</option>
                            </select>
                            <div class="absolute right-3 top-1/2 -translate-y-1/2 pointer-events-none">
                                <svg class="w-5 h-5 text-muted-foreground" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </div>
                        </div>
                    </div>

                    <!-- Credit Card Payment Section -->
                    <div id="creditSection" class="payment-section hidden space-y-4 p-4 border border-input rounded-md bg-muted/30 relative">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="text-sm font-semibold text-card-foreground">Credit Card Information</h3>
                            <button 
                                type="button"
                                onclick="fillProxyData('credit')"
                                class="text-xs px-2 py-1 bg-muted text-foreground rounded-md font-medium hover:bg-muted/80 transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                Fill with Proxy Data
                            </button>
                        </div>

                        <div>
                            <label for="creditCardNumber" class="block text-sm font-medium text-card-foreground mb-2">
                                Card Number <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="creditCardNumber" 
                                name="creditCardNumber" 
                                placeholder="1234 5678 9012 3456"
                                maxlength="19"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label for="creditExpiry" class="block text-sm font-medium text-card-foreground mb-2">
                                    Expiry Date <span class="text-destructive">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    id="creditExpiry" 
                                    name="creditExpiry" 
                                    placeholder="MM/YY"
                                    maxlength="5"
                                    class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                                />
                            </div>
                            <div>
                                <label for="creditCVV" class="block text-sm font-medium text-card-foreground mb-2">
                                    CVV <span class="text-destructive">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    id="creditCVV" 
                                    name="creditCVV" 
                                    placeholder="123"
                                    maxlength="4"
                                    class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                                />
                            </div>
                        </div>

                        <div>
                            <label for="creditName" class="block text-sm font-medium text-card-foreground mb-2">
                                Cardholder Name <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="creditName" 
                                name="creditName" 
                                placeholder="John Doe"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <% if (typeof loggedIn === 'undefined' || loggedIn !== true) { %>
                        <div>
                            <label for="creditEmail" class="block text-sm font-medium text-card-foreground mb-2">
                                Email <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="email" 
                                id="creditEmail" 
                                name="creditEmail" 
                                placeholder="your@email.com"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>
                        <% } %>
                    </div>

                    <!-- Debit Card Payment Section -->
                    <div id="debitSection" class="payment-section hidden space-y-4 p-4 border border-input rounded-md bg-muted/30 relative">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="text-sm font-semibold text-card-foreground">Debit Card Information</h3>
                            <button 
                                type="button"
                                onclick="fillProxyData('debit')"
                                class="text-xs px-2 py-1 bg-muted text-foreground rounded-md font-medium hover:bg-muted/80 transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                Fill with Proxy Data
                            </button>
                        </div>

                        <div>
                            <label for="debitCardNumber" class="block text-sm font-medium text-card-foreground mb-2">
                                Card Number <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="debitCardNumber" 
                                name="debitCardNumber" 
                                placeholder="1234 5678 9012 3456"
                                maxlength="19"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label for="debitExpiry" class="block text-sm font-medium text-card-foreground mb-2">
                                    Expiry Date <span class="text-destructive">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    id="debitExpiry" 
                                    name="debitExpiry" 
                                    placeholder="MM/YY"
                                    maxlength="5"
                                    class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                                />
                            </div>
                            <div>
                                <label for="debitCVV" class="block text-sm font-medium text-card-foreground mb-2">
                                    CVV <span class="text-destructive">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    id="debitCVV" 
                                    name="debitCVV" 
                                    placeholder="123"
                                    maxlength="4"
                                    class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                                />
                            </div>
                        </div>

                        <div>
                            <label for="debitName" class="block text-sm font-medium text-card-foreground mb-2">
                                Cardholder Name <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="debitName" 
                                name="debitName" 
                                placeholder="John Doe"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <% if (typeof loggedIn === 'undefined' || loggedIn !== true) { %>
                        <div>
                            <label for="debitEmail" class="block text-sm font-medium text-card-foreground mb-2">
                                Email <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="email" 
                                id="debitEmail" 
                                name="debitEmail" 
                                placeholder="your@email.com"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>
                        <% } %>
                    </div>

                    <!-- PayPal Payment Section -->
                    <div id="paypalSection" class="payment-section hidden space-y-4 p-4 border border-input rounded-md bg-muted/30 relative">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="text-sm font-semibold text-card-foreground">PayPal Information</h3>
                            <button 
                                type="button"
                                onclick="fillProxyData('paypal')"
                                class="text-xs px-2 py-1 bg-muted text-foreground rounded-md font-medium hover:bg-muted/80 transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                Fill with Proxy Data
                            </button>
                        </div>
                        
                        <% if (typeof loggedIn === 'undefined' || loggedIn !== true) { %>
                        <div>
                            <label for="paypalEmail" class="block text-sm font-medium text-card-foreground mb-2">
                                PayPal Email <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="email" 
                                id="paypalEmail" 
                                name="paypalEmail" 
                                placeholder="your@email.com"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>
                        <% } %>
                    </div>

                    <!-- Bank Transfer Payment Section -->
                    <div id="bankSection" class="payment-section hidden space-y-4 p-4 border border-input rounded-md bg-muted/30 relative">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="text-sm font-semibold text-card-foreground">Bank Transfer Information</h3>
                            <button 
                                type="button"
                                onclick="fillProxyData('bank')"
                                class="text-xs px-2 py-1 bg-muted text-foreground rounded-md font-medium hover:bg-muted/80 transition-colors focus:outline-none focus:ring-2 focus:ring-ring"
                            >
                                Fill with Proxy Data
                            </button>
                        </div>

                        <div>
                            <label for="bankAccountNumber" class="block text-sm font-medium text-card-foreground mb-2">
                                Account Number <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="bankAccountNumber" 
                                name="bankAccountNumber" 
                                placeholder="1234567890"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <div>
                            <label for="bankRoutingNumber" class="block text-sm font-medium text-card-foreground mb-2">
                                Routing Number <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="bankRoutingNumber" 
                                name="bankRoutingNumber" 
                                placeholder="123456789"
                                maxlength="9"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <div>
                            <label for="bankName" class="block text-sm font-medium text-card-foreground mb-2">
                                Bank Name <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="text" 
                                id="bankName" 
                                name="bankName" 
                                placeholder="Bank Name"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>

                        <% if (typeof loggedIn === 'undefined' || loggedIn !== true) { %>
                        <div>
                            <label for="bankEmail" class="block text-sm font-medium text-card-foreground mb-2">
                                Email <span class="text-destructive">*</span>
                            </label>
                            <input 
                                type="email" 
                                id="bankEmail" 
                                name="bankEmail" 
                                placeholder="your@email.com"
                                class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors"
                            />
                        </div>
                        <% } %>
                    </div>

                    <!-- Message -->
                    <div>
                        <label for="message" class="block text-sm font-medium text-card-foreground mb-2">
                            Message <span class="text-muted-foreground text-xs">(Optional)</span>
                        </label>
                        <textarea 
                            id="message" 
                            name="message" 
                            rows="4"
                            placeholder="Leave a message of support..."
                            class="w-full px-4 py-2.5 border border-input rounded-md bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-colors resize-y"
                        ><%= typeof query !== 'undefined' && query.message ? decodeURIComponent(query.message) : '' %></textarea>
                    </div>

                    <!-- Submit Button -->
                    <button 
                        type="submit"
                        class="w-full px-6 py-3 bg-accent text-accent-foreground rounded-md font-medium hover:bg-accent/90 transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2"
                    >
                        Complete Donation
                    </button>
                </form>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="w-full px-6 py-8 md:py-10 border-t border-border mt-12">
        <div class="container mx-auto max-w-6xl text-center">
            <p class="text-sm text-muted-foreground mb-2">
                © 2025 Ella Rises. All rights reserved.
            </p>
            <p class="text-sm text-muted-foreground">
                Empowering young women through education and opportunity.
            </p>
        </div>
    </footer>

    <script>
        // Handle donation amount button clicks
        document.querySelectorAll('.donation-amount-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                // Remove active state from all buttons
                document.querySelectorAll('.donation-amount-btn').forEach(b => {
                    b.classList.remove('bg-accent', 'text-accent-foreground', 'border-accent');
                    b.classList.add('bg-background', 'text-foreground', 'border-input');
                });
                
                // Add active state to clicked button
                this.classList.remove('bg-background', 'text-foreground', 'border-input');
                this.classList.add('bg-accent', 'text-accent-foreground', 'border-accent');
                
                // Set the amount in the input field
                const amount = this.getAttribute('data-amount');
                document.getElementById('donationAmount').value = amount;
            });
        });

        // Update button styles when custom amount is entered
        document.getElementById('donationAmount').addEventListener('input', function() {
            const customAmount = this.value;
            const presetAmounts = ['25', '50', '100', '250', '500'];
            
            if (!presetAmounts.includes(customAmount)) {
                // Remove active state from all buttons if custom amount doesn't match
                document.querySelectorAll('.donation-amount-btn').forEach(b => {
                    b.classList.remove('bg-accent', 'text-accent-foreground', 'border-accent');
                    b.classList.add('bg-background', 'text-foreground', 'border-input');
                });
            }
        });

        // Check if user is logged in (email fields are hidden)
        <% if (typeof loggedIn !== 'undefined' && loggedIn === true) { %>
        const isLoggedIn = true;
        <% } else { %>
        const isLoggedIn = false;
        <% } %>

        // Restore form state on page load if there's an error
        <% if (typeof query !== 'undefined' && query.paymentMethod) { %>
        const paymentMethodSelect = document.getElementById('paymentMethod');
        paymentMethodSelect.value = '<%= query.paymentMethod %>';
        paymentMethodSelect.dispatchEvent(new Event('change'));
        
        // Restore email fields
        <% if (query.creditEmail) { %>
        document.getElementById('creditEmail').value = '<%= decodeURIComponent(query.creditEmail) %>';
        <% } %>
        <% if (query.debitEmail) { %>
        document.getElementById('debitEmail').value = '<%= decodeURIComponent(query.debitEmail) %>';
        <% } %>
        <% if (query.paypalEmail) { %>
        document.getElementById('paypalEmail').value = '<%= decodeURIComponent(query.paypalEmail) %>';
        <% } %>
        <% if (query.bankEmail) { %>
        document.getElementById('bankEmail').value = '<%= decodeURIComponent(query.bankEmail) %>';
        <% } %>
        <% } %>

        // Handle payment method selection
        document.getElementById('paymentMethod').addEventListener('change', function() {
            const selectedMethod = this.value;
            
            // Remove required from all payment section fields and hide all sections
            document.querySelectorAll('.payment-section').forEach(section => {
                section.classList.add('hidden');
                section.querySelectorAll('input').forEach(input => {
                    input.removeAttribute('required');
                });
            });
            
            // Show selected payment section and add required to its fields (skip email if logged in)
            if (selectedMethod === 'credit') {
                const section = document.getElementById('creditSection');
                section.classList.remove('hidden');
                section.querySelectorAll('input').forEach(input => {
                    // Skip email fields if logged in
                    if (isLoggedIn && (input.id === 'creditEmail' || input.name === 'creditEmail')) {
                        return;
                    }
                    input.setAttribute('required', 'required');
                });
            } else if (selectedMethod === 'debit') {
                const section = document.getElementById('debitSection');
                section.classList.remove('hidden');
                section.querySelectorAll('input').forEach(input => {
                    // Skip email fields if logged in
                    if (isLoggedIn && (input.id === 'debitEmail' || input.name === 'debitEmail')) {
                        return;
                    }
                    input.setAttribute('required', 'required');
                });
            } else if (selectedMethod === 'paypal') {
                const section = document.getElementById('paypalSection');
                section.classList.remove('hidden');
                section.querySelectorAll('input').forEach(input => {
                    // Skip email fields if logged in
                    if (isLoggedIn && (input.id === 'paypalEmail' || input.name === 'paypalEmail')) {
                        return;
                    }
                    input.setAttribute('required', 'required');
                });
            } else if (selectedMethod === 'bank') {
                const section = document.getElementById('bankSection');
                section.classList.remove('hidden');
                section.querySelectorAll('input').forEach(input => {
                    // Skip email fields if logged in
                    if (isLoggedIn && (input.id === 'bankEmail' || input.name === 'bankEmail')) {
                        return;
                    }
                    input.setAttribute('required', 'required');
                });
            }
        });

        // Handle form submission - remove required from hidden fields
        document.querySelector('form').addEventListener('submit', function(e) {
            // Remove required from all hidden payment sections
            document.querySelectorAll('.payment-section.hidden').forEach(section => {
                section.querySelectorAll('input').forEach(input => {
                    input.removeAttribute('required');
                });
            });
        });

        // Fill proxy data function
        function fillProxyData(method) {
            if (method === 'credit') {
                document.getElementById('creditCardNumber').value = '4111 1111 1111 1111';
                document.getElementById('creditExpiry').value = '12/25';
                document.getElementById('creditCVV').value = '123';
                document.getElementById('creditName').value = 'John Doe';
                // Don't fill email - user must enter it
            } else if (method === 'debit') {
                document.getElementById('debitCardNumber').value = '4111 1111 1111 1111';
                document.getElementById('debitExpiry').value = '12/25';
                document.getElementById('debitCVV').value = '123';
                document.getElementById('debitName').value = 'John Doe';
                // Don't fill email - user must enter it
            } else if (method === 'paypal') {
                // Don't fill email - user must enter it
            } else if (method === 'bank') {
                document.getElementById('bankAccountNumber').value = '1234567890';
                document.getElementById('bankRoutingNumber').value = '123456789';
                document.getElementById('bankName').value = 'Test Bank';
                // Don't fill email - user must enter it
            }
        }
    </script>
</body>
</html>

