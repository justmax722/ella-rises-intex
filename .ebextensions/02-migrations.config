container_commands:
  01_run_migrations:
    command: "cd /var/app/ondeck && npm run migrate || echo 'Migration failed, will retry on app startup if needed'"
    leader_only: true
    ignoreErrors: true

